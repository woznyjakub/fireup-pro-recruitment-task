// style libraries are imported here to avoid sass variables conflict
import 'swiper/swiper.scss';
import '../styles/globals.scss';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
